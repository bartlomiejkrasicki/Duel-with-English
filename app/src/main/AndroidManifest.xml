<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pl.flanelowapopijava.angielski_slownictwo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings"
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="pl.flanelowapopijava.angielski_slownictwo.MainActivity" />
        </activity>
        <activity
            android:name=".VocabularyList"
            android:label="@string/app_name"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".LessonsVocabularyList"
            android:label="@string/title_activity_lessons_vocabulary_list"
            android:parentActivityName=".VocabularyList"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="pl.flanelowapopijava.angielski_slownictwo.VocabularyList" />
        </activity>
        <activity
            android:name=".FavouriteList"
            android:label="@string/title_activity_favourite_list"
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="pl.flanelowapopijava.angielski_slownictwo.MainActivity" />
        </activity>
    </application>

</manifest>